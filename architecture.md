# Architecture

The architecture write-up lives alongside the code. It covers the two runtime
phases, the dual-input flow, the layer boundaries, the domain model, and the
query lifecycle, with Mermaid diagrams:

**→ [`meeting-intelligence/docs/architecture.md`](meeting-intelligence/docs/architecture.md)**

It's kept in one place there rather than duplicated here, so it can't drift out of
sync with the code it describes.
