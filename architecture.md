# Architecture

The architecture write-up — the two runtime phases, the dual-input flow, the
layer boundaries, the domain model, and the query lifecycle, with Mermaid
diagrams — lives alongside the code:

**→ [`meeting-intelligence/docs/architecture.md`](meeting-intelligence/docs/architecture.md)**

(Kept as a single source there rather than duplicated here, so it can't drift out
of sync with the code it describes.)
